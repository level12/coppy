[build-system]
requires = [
    'hatchling',
    'hatch-regex-commit',
]
build-backend = 'hatchling.build'


[project]
name = '{{ project_name }}'
authors = [
    {name = '{{ author_name }}', email = '{{ author_email }}'},
]
{% if python_version_min == python_version -%}
requires-python = '~={{ python_version }}.0'
{% else -%}
requires-python = '>={{ python_version_min }}'
{% endif -%}
dynamic = ['version']
readme = 'readme.md'
{%- if script_name %}


[project.scripts]
'{{ script_name }}' = '{{ py_module }}.cli:main'
{%- endif %}


[dependency-groups]
dev = [
    {include-group = "pytest"},
    {include-group = "prek"},
    {include-group = "audit"},
    {include-group = "nox"},
    'click',
    'hatch',
    'ruff',
]
# Used by nox
pytest = [
    'pytest',
    'pytest-cov',
]
# Used by nox
prek = [
    'prek',
]
# Used by nox
audit = [
    'pip-audit',
]
# Used by CI
nox = [
    'nox',
]
